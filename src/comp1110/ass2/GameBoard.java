package comp1110.ass2;

public enum GameBoard {

    // Method 1 : private static String getSolution()
    // return the string that has the answer in sequence (b -> g -> i -> l -> n -> o -> p -> r -> s -> y)

    // Method 2 : getPlacementString()
    // return the String that has colour type + x_coordinate + y_coordinate + Direction
    // parameters :
    // return type: String
    // Example: possible output will be colour type is B, (x, y) = (3,2), Direction = NORTH
    // then the output will be "B32N".

    // Method 3 : private static String updateBoard()
    // parameters :
    // return type : String
    // returns the updated board every time a player puts down the piece.

    // Method 4 : changePlacement()
    // After playing the game and need to revert back, change the String placements.

    // This class is quite redundant, so remove this. Jiwon.

}
